services:
  bresser-bridge:
    build: .
    container_name: bresser_bridge
    restart: unless-stopped
    network_mode: host
    privileged: true
    environment:
      - LOG_LEVEL=INFO
      - MQTT_BROKER=IP
      - MQTT_TOPIC=bresser
      - ENABLE_ARP=true
      - TARGET_IP=IÜ
      - ROUTER_IP=IP
      - INTERFACE=eth0
    volumes:
      - ./certs:/app/certs